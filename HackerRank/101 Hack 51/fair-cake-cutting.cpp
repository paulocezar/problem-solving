#include <bits/stdc++.h>

using namespace std;

int main(int argc, char* argv[]) {

  ios::sync_with_stdio(false);
  cin.tie(NULL);

  int64_t A, B, a;
  cin >> A >> B >> a;
  cout << (a*B)/A << "\n";

  return 0;
}
